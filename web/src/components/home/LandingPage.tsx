import type { JSX } from "react";

export const LandingPage = (): JSX.Element => {
	return (
		<>
			<section className="inset-0 absolute bg-[url(./background-image.webp)] bg-no-repeat bg-cover -z-10 bg-center opacity-50 blur-lg">
				a
			</section>
		</>
	);
};
